services:
  nanobot:
    build: .
    container_name: nanobot
    restart: unless-stopped
    ports:
      - "18790:18790"
    command: gateway
    environment:
      - TZ=Asia/Seoul
      - NANOBOT_DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
      - ./workspace:/app/workspace
